name: "Hello World"
description: "Greet someone and record the time"
inputs:
  slicer:
    type: number
    required: true
  alchemy_api_key:
    required: true

runs:
  using: "node16"
  main: "dist/index.js"
