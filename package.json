{
  "name": "merge-to-earn",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ncc build index.js --license licenses.txt",
    "publish": "ncc build index.js --license licenses.txt && git add . && git commit -m 'v$npm_config_vtp' && git tag -a -m 'v$npm_config_vtp' v$npm_config_vtp && git push --follow-tags"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1"
  }
}
