{
  "name": "merge-to-earn",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p tsconfig.build.json",
    "publish": "tsc -p tsconfig.build.json && git add . && git commit -m 'v$npm_config_vtp' && git tag -a -m 'v$npm_config_vtp' v$npm_config_vtp && git push --follow-tags"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "@octokit/webhooks-definitions": "^3.67.3",
    "@types/node": "^18.8.4",
    "ethers": "^5.7.1",
    "typescript": "^4.8.4"
  }
}
